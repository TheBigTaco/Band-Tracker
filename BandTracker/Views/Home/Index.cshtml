@{
  Layout = "_Layout";
}

<h1>Bands</h1>

@if(Model.Count == 0)
{
  <h3>No bands added yet</h3>
}
@foreach(var band in Model)
{
  <p>@band.Name</p>
}

<p><a href="/band/add">Add a band</a></p>
<p><a href="/venue">View all venues</a></p>
